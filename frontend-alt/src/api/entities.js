import {
  registerAgent,
  delegate,
  validateDelegation,
  revokeDelegation,
  getAuditLogs,
} from './proxymeApi';

export { registerAgent, delegate, validateDelegation, revokeDelegation, getAuditLogs };
